node_modules/
dist/

# Secrets live in *.local only — Vite's convention. Committed .env files hold
# public config (the room server URL), which is not a secret and which makes a
# fresh deploy work with no dashboard setup.
.env
.env.local
.env.*.local

*.log
.DS_Store
